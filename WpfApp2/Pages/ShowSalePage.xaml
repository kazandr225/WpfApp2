<Page x:Class="WpfApp2.Pages.ShowSalePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ShowSalePage">

    <Grid>
        <ListView Name="listSale" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Center"></WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            
            <ListView.ItemTemplate>
            <DataTemplate>
                    <Border Padding="5" CornerRadius="5" BorderThickness="1" BorderBrush="Aquamarine">
                        <Grid Name="gridMain" Width="500" Height="500" Background="{Binding GenderColor}">
                            <StackPanel>
                                <TextBlock Text="{Binding Product.Name_Product}"/>
                                <!--<TextBlock Uid="{Binding id_Sale}" Name="tbAmountSale" Loaded="tbAmountSale_Loaded"></TextBlock>-->
                                <TextBlock Text="{Binding Employee.Surname}"></TextBlock>
                                <TextBlock Text="{Binding Employee.Name}"></TextBlock>
                                <TextBlock Text="{Binding Employee.Patronymic}"></TextBlock>
                                
                                
                            </StackPanel> 
                        </Grid>
                    </Border>
            </DataTemplate>
            
            </ListView.ItemTemplate>
        </ListView>
        <Button HorizontalAlignment="Left" Height="35" VerticalAlignment="Top" Name="btnCreateSale" Click="btnCreateSale_Click">Добавить продажу</Button>
    </Grid>
</Page>
