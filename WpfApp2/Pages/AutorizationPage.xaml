<Page x:Class="WpfApp2.Pages.AutorizationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AutorizationPage" FontSize="18">

    <Grid>
        <StackPanel>
            <GroupBox Header="Введите логин">
                <TextBox Name="tboxLogin">Логин</TextBox>
            </GroupBox>
            <GroupBox Header="Введите пароль">
                <PasswordBox Name="pbPasword" Password="Пароль"></PasswordBox>
            </GroupBox>
            <Button Click="btnAutorization_Click">Авторизация</Button>
            <Button Name="btnBackMain" Click="btnBackMain_Click">Назад на главную</Button>
        </StackPanel>
    </Grid>
</Page>
